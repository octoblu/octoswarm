version: '1.0'
steps:

  build_image:
    type: build
    description: Building the image...
    working_directory: ${{main_clone}}
    dockerfile: Dockerfile-1.12.6
    image_name: ${{CF_REPO_OWNER}}/${{CF_REPO_NAME}}
    tag: 1.12.6

  push_to_registry:
    title: Pushing to Docker Registry (with credentials)
    type: push
    candidate: '${{build_image}}'
    tag: 1.12.6
